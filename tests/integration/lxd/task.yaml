summary: Run workspace backend LXD integration tests
prepare: |


restore: |
  . "$TESTSLIB"/utils.sh
  cleanup "$WORKSPACES"

execute: |
  . "$TESTSLIB"/utils.sh
  go test -check.v --tags=integration
