summary: sdkcraft tutorial test

execute: |
  run_sdkcraft --help
  mkdir go/
  cd go/
  run_sdkcraft init
  mv ../sdkcraft.yaml .
  mkdir hooks
  mv ../{setup-base,save-state,restore-state,check-health} hooks/
  chmod +x hooks/*
  run_sdkcraft clean
  run_sdkcraft
