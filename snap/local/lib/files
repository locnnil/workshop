install_file() {
    local mode src tgt name tmp
    mode="$1"
    src="$2"
    tgt="$3"

    install --directory -- "$tgt"
    name="$(basename -- "$src")"

    tmp="$(mktemp -- "${tgt}/${name}.XXXXXXXXXX")"
    cp -- "$src" "$tmp"
    chmod "$mode" -- "$tmp"
    mv -- "$tmp" "${tgt}/${name}"
}
